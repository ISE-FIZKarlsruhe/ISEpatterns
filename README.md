# ISE Ontology Design Patterns

To work on the docs and contents, do:

```shell
pip install mkdocs-material
```

And then you can run:

```shell
mkdocs serve
```

And this gives you live documentation in your browser at: http://localhost:8000/
